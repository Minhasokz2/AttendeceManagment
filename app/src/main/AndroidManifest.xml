<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.attendecemanagment">


    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".student.student_attendence_enterPin"></activity>
        <activity android:name=".activities.attendence_verification" />
        <activity android:name=".student.student_scanQr" />
        <activity android:name=".activities.activity_subjectAttendence" />
        <activity android:name=".activities.activity_studentProfileDetail" />
        <activity android:name=".activities.activity_profile" />
        <activity android:name=".activities.user_attendence_firstActivity" />
        <activity android:name=".activities.activity_teacher_generateCode" />
        <activity android:name=".student.StudentAttendence_RecycleView" />
        <activity android:name=".activities.teacher_activity_Attendence" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>